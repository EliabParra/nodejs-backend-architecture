# BO (Business Objects)

This folder is intentionally empty.

- Add your domain BOs here (e.g. `BO/ObjectName/ObjectNameBO.js`).
- Use the BO CLI to scaffold: `npm run bo -- new ObjectName`.

Demo BOs (if you want to see examples) live under `examples/bo-demo/BO/`.
