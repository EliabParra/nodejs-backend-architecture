export class Prueba {
    constructor() {}

    probar1(msg) {
        return { msg: `Mensaje de prueba 1 ${msg}` }
    }

    probar2(msg) {
        return { msg: `Mensaje de prueba 2 ${msg}` }
    }
}
